name: Build and Publish Talos CM4/CM5 Image

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      talos_version:
        description: 'Base Talos version (e.g. v1.12.6)'
        required: false
        default: ''

jobs:
  # ── Resolve the Talos version once and share it across all jobs ─────────────
  resolve-version:
    runs-on: ubuntu-24.04-arm
    outputs:
      talos_version: ${{ steps.version.outputs.version }}
      patched_release_tag: ${{ steps.version.outputs.patched_release_tag }}
      installer_base: ${{ steps.version.outputs.installer_base }}
      overlay_image: ${{ steps.version.outputs.overlay_image }}
      linux_kernel_version: ${{ steps.version.outputs.linux_kernel_version }}
      iscsi_version: ${{ steps.version.outputs.iscsi_version }}
      util_version: ${{ steps.version.outputs.util_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve Talos version and metadata
        id: version
        run: |
          if [[ -n "${{ github.event.inputs.talos_version }}" ]]; then
            VERSION="${{ github.event.inputs.talos_version }}"
          elif [[ "${{ github.ref_type }}" == "tag" ]]; then
            VERSION="${{ github.ref_name }}"
          else
            VERSION=$(grep '^TALOS_VERSION' Makefile | awk -F'?=' '{print $2}' | tr -d ' ')
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "patched_release_tag=${VERSION}-rpi-kernel" >> "$GITHUB_OUTPUT"
          ISCSI=$(grep '^ISCSI_TOOLS_VERSION' Makefile | awk -F'?=' '{print $2}' | tr -d ' ')
          UTIL=$(grep '^UTIL_LINUX_VERSION' Makefile | awk -F'?=' '{print $2}' | tr -d ' ')
          LINUX=$(grep '^LINUX_KERNEL_VERSION' Makefile | awk -F'?=' '{print $2}' | tr -d ' ')
          INSTALLER_BASE=$(grep '^CUSTOM_INSTALLER_BASE' Makefile | awk -F'?=' '{print $2}' | sed 's/^ *//' | tr -d ' ')
          OVERLAY_IMAGE=$(grep '^CUSTOM_OVERLAY_IMAGE' Makefile | awk -F'?=' '{print $2}' | sed 's/^ *//' | tr -d ' ')
          echo "overlay_image=${OVERLAY_IMAGE}" >> "$GITHUB_OUTPUT"
          echo "linux_kernel_version=${LINUX}" >> "$GITHUB_OUTPUT"
          echo "installer_base=${INSTALLER_BASE}" >> "$GITHUB_OUTPUT"
          echo "iscsi_version=${ISCSI}" >> "$GITHUB_OUTPUT"
          echo "util_version=${UTIL}" >> "$GITHUB_OUTPUT"


  # ── Build image + installer ────────────────────────────────────────────────────────────────────└
  build:
    runs-on: ubuntu-24.04-arm
    needs: [resolve-version]
    permissions:
      contents: read
      packages: write
    env:
      DOCKER: docker
      TALOS_VERSION: ${{ needs.resolve-version.outputs.talos_version }}
      PATCHED_RELEASE_TAG: ${{ needs.resolve-version.outputs.patched_release_tag }}
      REGISTRY: ghcr.io
      REGISTRY_USERNAME: ${{ github.repository_owner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.repository_owner }}" --password-stdin

      - name: Install dependencies
        run: sudo apt-get install -y xz-utils

      - name: Build Talos image
        run: make build TALOS_VERSION=${{ env.TALOS_VERSION }} CUSTOM_INSTALLER_BASE=${{ needs.resolve-version.outputs.installer_base }} CUSTOM_OVERLAY_IMAGE=${{ needs.resolve-version.outputs.overlay_image }}

      - name: Build installer
        run: make installer TALOS_VERSION=${{ env.TALOS_VERSION }} CUSTOM_INSTALLER_BASE=${{ needs.resolve-version.outputs.installer_base }} CUSTOM_OVERLAY_IMAGE=${{ needs.resolve-version.outputs.overlay_image }}

      - name: Push installer to GHCR
        run: make push-installer TALOS_VERSION=${{ env.TALOS_VERSION }} INSTALLER_TAG=${{ env.PATCHED_RELEASE_TAG }} CUSTOM_INSTALLER_BASE=${{ needs.resolve-version.outputs.installer_base }}

      - name: Upload disk image
        uses: actions/upload-artifact@v4
        with:
          name: metal-arm64
          path: _out/metal-arm64.raw.xz

  # ── Create GitHub release ───────────────────────────────────────────────────
  release:
    runs-on: ubuntu-24.04-arm
    needs: [resolve-version, build]
    permissions:
      contents: write
    env:
      TALOS_VERSION: ${{ needs.resolve-version.outputs.talos_version }}
      PATCHED_RELEASE_TAG: ${{ needs.resolve-version.outputs.patched_release_tag }}
      INSTALLER_BASE: ${{ needs.resolve-version.outputs.installer_base }}
      OVERLAY_IMAGE: ${{ needs.resolve-version.outputs.overlay_image }}
      LINUX_KERNEL_VERSION: ${{ needs.resolve-version.outputs.linux_kernel_version }}
      ISCSI_VERSION: ${{ needs.resolve-version.outputs.iscsi_version }}
      UTIL_VERSION: ${{ needs.resolve-version.outputs.util_version }}
      REGISTRY: ghcr.io
      REGISTRY_USERNAME: ${{ github.repository_owner }}
      GH_REPO: ${{ github.repository }}
    steps:
      - name: Download disk image artifact
        uses: actions/download-artifact@v4
        with:
          name: metal-arm64
          path: _out/

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          NOTES=$(cat <<EOF
          > ⚠️ Experimental build, use at your own risk.

          This is a patched Talos build for **Raspberry Pi CM4 and CM5** boards.
          Uses the `rpi_generic` overlay (PR #88) which supports CM4IO/CM5IO carriers and Pi 5.

          ### Components

          | Component | Version / Image |
          |-----------|------------------|
          | Imager | \`ghcr.io/siderolabs/imager:${{ env.TALOS_VERSION }}\` |
          | Installer base | \`${{ env.INSTALLER_BASE }}\` |
          | Talos | \`${{ env.TALOS_VERSION }}\` |
          | Kernel | Linux ${{ env.LINUX_KERNEL_VERSION }} — prebuilt vendor-kernel via \`${{ env.INSTALLER_BASE }}\` |
          | SBC overlay | \`${{ env.OVERLAY_IMAGE }}\` (PR #88 — BCM2712/RP1 U-Boot + NVMe, rpi_generic) |
          | iscsi-tools | \`ghcr.io/siderolabs/iscsi-tools:${{ env.ISCSI_VERSION }}\` |
          | util-linux-tools | \`ghcr.io/siderolabs/util-linux-tools:${{ env.UTIL_VERSION }}\` |

          ### Supported boards

          - Raspberry Pi CM5 (CM5IO, CM4IO-compatible carriers e.g. DeskPi Super6C)
          - Raspberry Pi CM4 (CM4IO and compatible carriers)
          - Raspberry Pi 5

          ### What's available

          - 📦 **\`metal-arm64.raw.xz\`** — disk image for CM4/CM5/Pi5
          - ⚙️  **Installer:** \`${{ env.REGISTRY }}/${{ env.REGISTRY_USERNAME }}/talos-rpi-cm5-installer:${{ env.PATCHED_RELEASE_TAG }}\`

          ### Install

          - **Fresh install**
            - Download \`metal-arm64.raw.xz\` from this release
            - Flash with \`dd\` or your favorite tool

          - **Upgrade existing node**
            \`\`\`bash
            talosctl upgrade --nodes <NODE_IP> --image ${{ env.REGISTRY }}/${{ env.REGISTRY_USERNAME }}/talos-rpi-cm5-installer:${{ env.PATCHED_RELEASE_TAG }}
            \`\`\`

          EOF
          )
          gh release delete ${{ env.PATCHED_RELEASE_TAG }} --yes 2>/dev/null || true
          gh release create \
            ${{ env.PATCHED_RELEASE_TAG }} \
            ./_out/metal-arm64.raw.xz \
            --title "${{ env.PATCHED_RELEASE_TAG }}" \
            $([[ "${{ env.TALOS_VERSION }}" =~ -(alpha|beta|rc)\. ]] && echo "--prerelease") \
            --notes "$NOTES"
